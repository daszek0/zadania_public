// Daszek
// Reversort Engineering

#include <cstdio>
#include <algorithm>

using namespace std;

int main()
{
	int t, n, c;
	scanf("%d", &t);
	for (int i = 1; i <= t; i++)
	{
		scanf("%d %d", &n, &c);
		int l[n];
		for (int i = 1; i <= n; i++)
		{
			l[i] = i;
		}
		
	}
	return 0;
}